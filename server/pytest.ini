[pytest]
testpaths = tests
# pytest는 테스트 파일의 basedir(/app/tests)를 sys.path에 넣는다.
# app 패키지를 임포트하려면 rootdir(/app)을 추가해야 한다.
pythonpath = .
python_files = test_*.py
addopts = -v --tb=short --strict-markers
filterwarnings =
    error::DeprecationWarning:app.*
