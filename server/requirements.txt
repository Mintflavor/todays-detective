# 작성자 : 박현일
# 이 코드의 소유권은 작성자에게 있으며 아래 코드의 일부 또는 전체는 AI(Claude, Gemini)를 활용하여 작성되었습니다.
#
# Author: Hyunil Park
# Ownership of this code belongs to the author, and some or all of the code below has been written using AI (Claude, Gemini).
#
# 버전은 PyPI 실측(2026-08-25) 기준으로 고정한다. 추측한 핀은 빌드를 깨뜨린다.

fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.4
pydantic-settings==2.15.0

pymongo==4.17.0

# lambda/requirements.txt는 google-genai==0.3.0(2024년 말)에 고정돼 있었다.
# types.ImageConfig 와 Gemini 3 이미지 모델을 쓰려면 최신이어야 한다. (계획 §0-F)
google-genai==2.19.0

boto3==1.43.79
Pillow==12.3.0

# 레이트 리밋 — Phase 5.5는 배포와 동시에 적용해야 하므로 미리 넣는다.
slowapi==0.1.10
